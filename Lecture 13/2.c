#include <stdio.h>

int main() {
    char singleChar = 'A'; // Example of a single character
    char nextChar = singleChar + 1; // ASCII manipulation

    printf("Character: %c\n", singleChar);
    printf("Next Character: %c\n", nextChar);

    return 0;
}