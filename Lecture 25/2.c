#include <stdio.h> 

int main() { 
    int inputNumber; 
    printf("Enter a number: "); 
    scanf("%d", &inputNumber); // Reading user input 
    printf("You entered: %d\n", inputNumber); 
    return 0; 
}