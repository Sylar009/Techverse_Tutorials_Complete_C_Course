#include <stdio.h>
int main() {
    const int constantValue = 50; // Declaring a constant
    printf("Initial Value: %d\n", constantValue);

    // Uncommenting the next line will cause a compiler error
    // constantValue = 100; 

    return 0;
}