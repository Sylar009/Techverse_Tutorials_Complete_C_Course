#include <stdio.h>

int main() {
    int normalInt = -10;
    unsigned int unsignedInt = 4294967295;

    printf("normalInt: %d\n", normalInt);
    printf("unsignedInt: %u\n", unsignedInt);

    return 0;
}